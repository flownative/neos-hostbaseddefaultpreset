{
    "name": "flownative/neos-hostbaseddefaultpreset",
    "type": "neos-package",
    "description": "This allows to have the default preset for content dimensions change depending on the hostname of a site",
    "license": "MIT",
    "require": {
        "neos/neos": "~3.0 || ~4.0 || ~5.0 || ~7.0 || ~8.0 || dev-master"
    },
    "autoload": {
        "psr-4": {
            "Flownative\\Neos\\HostBasedDefaultPreset\\": "Classes"
        }
    }
}
